/*
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Deque<Integer> myDeque = new LinkedList<Integer>();

        myDeque.add(1); // Добавляем последний элемент
        myDeque.addFirst(2); // Добавляем элемент в начало
        myDeque.addLast(3); // Добавляем элемент в конец
        myDeque.push(4); // Добавляем в начало
        myDeque.offer(5); // Добавляем в конец
        myDeque.offerFirst(6); // Добавляем в начало

        System.out.println(myDeque + "\n"); // Выводим

        // Удаляем первый и посдений элементы
        myDeque.removeFirst();
        myDeque.removeLast();

        // Выводим данные после удаления
        System.out.println("Данные после удаления: " + myDeque);
    }
}
 */